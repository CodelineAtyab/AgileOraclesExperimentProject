Converting a program to a JAR (Java ARchive) bundles your compiled code and all its required libraries into a single portable file. Instead of managing separate class files and library folders, you get one self-contained file that can run on any computer with Java installed.

To run it: `java -jar Weather.jar`
