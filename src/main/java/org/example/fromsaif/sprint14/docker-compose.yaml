services:
  api:
    build: .
    image: healthcheck-app:1.0.0
    container_name: healthcheck-compose
    ports:
      - "8080:8080"
    volumes:
      - ./shared_upload_files:/app/uploaded_files
    restart: always
