services:
  api:
    build: .
    image: healthcheck-app
    container_name: healthcheck-container
    ports:
      - "8000:8080"
    volumes:
      - ./shared_upload_files:/app/uploaded_files
    restart: always
