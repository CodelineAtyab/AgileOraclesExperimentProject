services:
  spring-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: demo-app:latest
    container_name: demo-container
    ports:
      - "8080:8080"
    restart: unless-stopped

