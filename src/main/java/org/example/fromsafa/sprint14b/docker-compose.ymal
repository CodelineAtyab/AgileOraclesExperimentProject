services:
      api:
        build: .
        image: safalsibani/health-app:1.0.0
        container_name: "health-app"
        ports:
          - 8000:8080
        volumes:
          - ./shared_upload_files:/app/uploaded_files
        restart: always
